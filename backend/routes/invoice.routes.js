const express = require('express');
const router = express.Router();

// Placeholder for invoice endpoints
router.get('/placeholder', (req, res) => {
  res.json({ message: 'Invoice routes placeholder' });
});

module.exports = router;

const express = require('express');
const router = express.Router();

// Placeholder for quotation endpoints
router.get('/placeholder', (req, res) => {
  res.json({ message: 'Quotation routes placeholder' });
});

module.exports = router;

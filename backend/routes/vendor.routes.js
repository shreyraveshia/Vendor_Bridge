const express = require('express');
const router = express.Router();

// Placeholder for vendor endpoints
router.get('/placeholder', (req, res) => {
  res.json({ message: 'Vendor routes placeholder' });
});

module.exports = router;

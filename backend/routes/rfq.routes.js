const express = require('express');
const router = express.Router();

// Placeholder for RFQ endpoints
router.get('/placeholder', (req, res) => {
  res.json({ message: 'RFQ routes placeholder' });
});

module.exports = router;
